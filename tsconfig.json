// Iniciar um projeto expo com Bare Workflow necessita fazer a configuração do TypeScript
// Para isso deve-se cirar na raiz um arquivo tscofig.json com os seguintes parametros

{
  "compilerOptions": {
      "strict": true
  },
  "extends": "expo/tsconfig.base"
}

//Após isso basta mudar a extenção do aquivo App de .js para .tsx
//Todos os arquivos depois disso já podem ser criados com .tsx
